% Long enum
\newlist{longenum}{enumerate}{8}
\setlistdepth{8}
\setlist[longenum,1]{label=\roman*)}
\setlist[longenum,2]{label=\alph*)}
\setlist[longenum,3]{label=\arabic*)}
\setlist[longenum,4]{label=(\roman*)}
\setlist[longenum,5]{label=(\alph*)}
\setlist[longenum,6]{label=(\alph*)}
\setlist[longenum,7]{label=(\alph*)}
\setlist[longenum,8]{label=(\alph*)}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Define commands for enumeration
% Generic enum
%\newcommand{\gene}[2]{
%	\begin{longenum}[%
%		label*=#1,%
%		topsep=1em,%
%		itemsep=1em,%
%		partopsep=1em,%
%		parsep=1em,%
%		labelsep=1em,%
%		leftmargin=#2
%	]
%}
\newcommand{\gene}[2]{
	\begin{longenum}[%
		labelindent=0pt,%
		label*=#1,%
		itemindent=0em,%
%		leftmargin=!,%
		topsep=0em,%
		itemsep=#2,%
		partopsep=0em,%
		parsep=1em,%
		labelsep=1em,%
		leftmargin=2em,
	]
}

\newcommand{\genee}[2]{
	\begin{longenum}[%
		labelindent=0pt,%
		label=#1,%
		itemindent=0em,%
%		leftmargin=!,%
		topsep=0em,%
		itemsep=#2,%
		partopsep=0em,%
		parsep=1em,%
		labelsep=1em,%
		leftmargin=2em,
	]
}
\newcommand{\gener}[2]{
	\begin{longenum}[%
		resume,
		labelindent=0pt,%
		label*=#1,%
		itemindent=0em,%
%		leftmargin=!,%
		topsep=0em,%
		itemsep=#2,%
		partopsep=0em,%
		parsep=1em,%
		labelsep=1em,%
		leftmargin=2em,
	]
}

\newcommand{\geneer}[2]{
	\begin{longenum}[%
		resume,
		labelindent=0pt,%
		label=#1,%
		itemindent=0em,%
%		leftmargin=!,%
		topsep=0em,%
		itemsep=#2,%
		partopsep=0em,%
		parsep=1em,%
		labelsep=1em,%
		leftmargin=2em,
	]
}

% Leveled enums
\newcommand{\e}{\gene{{\alph*}.}{1em}}
\newcommand{\ee}{\gene{{\arabic*}}{1em}}
\newcommand{\eee}{\gene{({\alph*})}{1em}}
\newcommand{\eeee}{\genee{({\arabic*})}{1em}}
\newcommand{\eeeee}{\genee{{\Roman*}}{1em}}
\newcommand{\eeeeee}{\genee{({\roman*})}{1em}}
\newcommand{\eeeeeee}{\genee{({\arabic*})}{1em}}


% resume
\newcommand{\re}{\gener{{\alph*}.}{1em}}
\newcommand{\ree}{\gener{{\arabic*}}{1em}}
\newcommand{\reee}{\gener{({\alph*})}{1em}}
\newcommand{\reeee}{\geneer{({\arabic*})}{1em}}
\newcommand{\reeeee}{\geneer{{\Roman*}}{1em}}
\newcommand{\reeeeee}{\geneer{({\roman*})}{1em}}
\newcommand{\reeeeeee}{\geneer{({\arabic*})}{1em}}

% terminate
\newcommand{\ed}{\end{longenum}}

% Environment
\newenvironment{e1}[1][2em]{\gene  { {\alph*}.    } {#1} } {\end{longenum}}
\newenvironment{e2}[1][2em]{\gene  { {\arabic*}   } {#1} } {\end{longenum}}
\newenvironment{e3}[1][2em]{\gene  { ({\alph*})   } {#1} } {\end{longenum}}
\newenvironment{e4}[1][2em]{\genee { ({\arabic*}) } {#1} } {\end{longenum}}
\newenvironment{e5}[1][1em]{\genee { {\Roman*}    } {#1} } {\end{longenum}}
\newenvironment{e6}[1][1em]{\genee { ({\roman*})  } {#1} } {\end{longenum}}
\newenvironment{e7}[1][1em]{\genee { ({\arabic*}) } {#1} } {\end{longenum}}
%\newenvironment{e8}{\eeeeeeee}{\ed}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Title item
\newcommand{\itemt}[2]{
\parbox{\linewidth}{\item #1\\[1em]#2}
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Glossary enum
\newlist{glossaryenum}{enumerate}{8}
\setlistdepth{8}
\setlist[glossaryenum,1]{label=\roman*)}
\setlist[glossaryenum,2]{label=\alph*)}
\setlist[glossaryenum,3]{label=\arabic*)}
\setlist[glossaryenum,4]{label=(\roman*)}
\setlist[glossaryenum,5]{label=(\alph*)}
\setlist[glossaryenum,6]{label=(\alph*)}
\setlist[glossaryenum,7]{label=(\alph*)}
\setlist[glossaryenum,8]{label=(\alph*)}

\newcommand{\glene}[2]{
	\begin{glossaryenum}[%
		labelindent=0pt,%
		label*=#1,%
		itemindent=0em,%
%		leftmargin=!,%
		topsep=0em,%
		itemsep=0em,%
		partopsep=0em,%
		parsep=0em,%
		labelsep=1em,%
		leftmargin=2.2em,
	]
}

\newcommand{\gle}{\glene{{\alph*}.}{2em}}

\newcommand{\gled}{\end{glossaryenum}}